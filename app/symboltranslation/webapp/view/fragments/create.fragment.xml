<core:FragmentDefinition xmlns:smartfield="sap.ui.comp.smartfield"
    xmlns="sap.m"
    xmlns:f="sap.ui.layout.form"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns:core="sap.ui.core">
    <!-- HACK THE FUTURE Challenge: several properties are missing to make this a succesful save-->
    <Dialog id="_IDGenDialog" title="Add new symbol" draggable="true">
        <content>
            <VBox class="sapUiSmallMargin" id="formContainer">
                <f:SimpleForm id="SimpleFormChangeColumn_twoGroups234"
                              editable="true"
                              layout="ColumnLayout"
                              columnsM="2" columnsL="2" columnsXL="2">
                    <f:content>
                        <Label text="Icon" required="true"/>
                        <Input value="{create>/symbol}" required="true"/>

                        <Label text="Where abouts" required="true"/>
                        <Input value="{create>/whereFound}" required="true"/>

                        <Label text="Language" required="true"/>
                        <ComboBox
                            selectedKey="{create>/language}"
                            items="{languages>/}">
                            <core:Item key="{languages>language}" text="{languages>language}" />
                        </ComboBox>
                    </f:content>
                </f:SimpleForm>
            </VBox>
        </content>

        <footer>
            <Toolbar>
                <content>
                    <ToolbarSpacer/>
                    <Button text="Save" type="Emphasized" press="save"/>
                    <Button text="Cancel" type="Reject" press="closeDialog"/>
                </content>
            </Toolbar>
        </footer>
    </Dialog>
</core:FragmentDefinition>
